syntax = "proto3";

package distributionSystemService;

service DistributionSystemService {
    rpc Ping(Empty) returns (PingResponse);
}

message Empty {}

message PingResponse {
  bool is_alive = 1;
}
